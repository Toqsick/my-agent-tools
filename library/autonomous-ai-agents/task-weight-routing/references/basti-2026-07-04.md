# Basti's Model-Routing-Referenz (Stand: 2026-07-04, v2)

## Claude Code Pro — Verfügbare Modelle (Juli 2026)

Bestätigt via `claude --help` und `claude doctor`:

| Alias | Vollname | Typ | Budget | Turns | Besonderheit |
|-------|----------|-----|--------|-------|-------------|
| `fable` | `claude-fable-5` | Heavy (Rabat!) | **KEIN Limit** | **KEIN Limit** | Bis 7.7. mit 50% Rabat — Basti will Volle Power |
| `sonnet` | `claude-sonnet-5` | Medium | $0.50 | 10 | Standard für Refactors |
| `opus` | `claude-opus-4-8` | Heavy+ | $2.00 | 30 | Nur auf Basti's Anweisung |
| `haiku` | `claude-haiku-4-5` | Light | $0.20 | 5 | Mini-Edits, Lookups |

## Rabat-Bulletin (bis 2026-07-07)

- **50% Rabat** auf Pro-Subscription für Fable 5
- Konto: `bastick123@gmail.com` (Pro-OAuth)
- **KEIN** API-Key, **KEIN** `provider: anthropic` in Hermes config
- **AKTIONSREGEL (2026-07-04 Korrektur):** Fable 5 am Start **KEIN Budget-Limit** geben. Basti sagt: "sei am anfang jetzt nich geizig" und "gib am anfang kein limit vor". Erst wenn Task konkret Budget-Schätzung hat oder Basti "sparsam" sagt, Limits setzen.
- **Nach Rabat-Ende (8.7.):** Normales Pro-Budget, Fable 5 dann wieder $0.80/15t als Default

## Basti's Orchestrierungs-Preference

> "Du kontrollierst den Verbrauch und orchestrierst. Wenn ich 'mit Fable/Opus/Sonnet' sage, wählst du klug je nach Task-Gewicht."

### Trigger → Aktion (vollständige Map)

| Basti sagt | `--model` | Budget/Turns | Wofür |
|------------|-----------|-------------|-------|
| "mit Fable" | `fable` | KEIN Limit | Heavy Refactor, Big Analysis, Audits |
| "mit Sonnet" | `sonnet` | $0.50 / 10 | Code-Review, Medium Refactor |
| "mit Opus" | `opus` | $2.00 / 30 | Architecture, Critical Code |
| (implizit) | `haiku` | $0.20 / 5 | Mini-Edits, Lookups |

### Fable 5 Delegation (mit Background + notify)

```python
# KEIN --max-turns, KEIN --max-budget-usd
# Briefing als Datei, NICHT per Pipe
cmd = 'claude -p "$(cat /tmp/brief.txt)" --model fable'
terminal(background=True, notify_on_complete=True, command=cmd, timeout=300)
```

### Sonnet/Normal (mit Limits)

```python
cmd = 'claude -p "<task>" --model claude-sonnet-5 --max-turns 10 --max-budget-usd 0.50'
terminal(command=cmd, workdir="/path", timeout=120)
```

## Session History (2026-07-04)

- **Fable Smoke 1:** Brief per `cat | claude -p` → Self-Pipe-Bug, Budget verbrannt, kein Output
- **Fable Smoke 2:** `--max-turns 8 --max-budget-usd 1.50` → Basti brach ab ("gib kein limit vor")
- **Fable Task 1 (laufend):** Skill-Inventur via `-p "$(cat /tmp/brief.txt)" --model fable` (KEIN Limit)
- **Fable Task 2 (laufend):** Home-Cleanup-Analyse via gleichem Pattern (KEIN Limit)

## Wichtige Metadaten

| Property | Wert |
|----------|------|
| Auth-Typ | Pro-OAuth (claude.ai), subscriptionType: "pro" |
| CLI-Pfad | `/home/bratan/.local/bin/claude` |
| CLI-Version | v2.1.201 |
| Hermes-Config | **KEIN** `provider: anthropic` — Claude Code läuft nur via `terminal()` |
| Rabat-Gültig | 2026-07-04 bis 2026-07-07 |
| GitHub-Repos | Basti's Territorium (eigener Plan) — NICHT delegieren |
| GreyHack | RAUS aus diesem Rabat-Burn — NICHT anrühren |
