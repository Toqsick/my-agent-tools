# Cross-References

This skill relates to and complements:

| Skill | Purpose |
|-------|---------|
| `hermes-agent` | Core Hermes configuration, CLI commands, and setup |
| `dashboard-deployment` | Production Nginx + SSL + OAuth deployment for the dashboard |
| `hermes-gateway` | Messaging platform gateway setup (if exists) |

## Quick Links

- **Dashboard deployment**: See `dashboard-deployment` skill for Nginx, Certbot, systemd templates
- **Memory provider**: This skill covers Ariadne as `memory.provider: ariadne`
- **Hermes CLI**: `hermes dashboard register`, `hermes plugins enable ariadne`, `hermes config set memory.provider ariadne`